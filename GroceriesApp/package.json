{
  "name": "GroceriesApp",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    // Start the development server for Android
    "android": "react-native run-android",
    
    // Start the development server for iOS
    "ios": "react-native run-ios",
    
    // Run ESLint to check code for linting issues
    "lint": "eslint .",
    
    // Start the React Native development server
    "start": "react-native start",
    
    // Run Jest for testing
    "test": "jest",
    
    // The following build and install scripts are not required for React Native on Heroku
    // They are provided here for clarity, but you can leave them as they are
    // or remove them if you prefer
    "build": "echo 'Build step is not required'",
    "install": "npm install"
  },
  "dependencies": {
    //dependencies
    "axios": "^1.4.0",
    "react": "18.2.0",
    "react-native": "^0.72.3",
    "react-native-share": "^9.2.3"
  },
  "devDependencies": {
    //dev dependencies
    "@babel/core": "^7.20.0",
    "@babel/preset-env": "^7.20.0",
    "@babel/runtime": "^7.20.0",
    "@react-native/eslint-config": "^0.72.2",
    "@react-native/metro-config": "^0.72.9",
    "@tsconfig/react-native": "^3.0.0",
    "@types/react": "^18.0.24",
    "@types/react-test-renderer": "^18.0.0",
    "babel-jest": "^29.2.1",
    "eslint": "^8.19.0",
    "jest": "^29.2.1",
    "metro-react-native-babel-preset": "0.76.7",
    "prettier": "^2.4.1",
    "react-test-renderer": "18.2.0",
    "typescript": "4.8.4"
  },
  "engines": {
    // Specify compatible Node.js version range
    "node": ">=16 <=20"
  },
  "buildpacks": [
    // Specify the Heroku buildpack to use
    {
      "url": "heroku/nodejs"
    }
  ]
}
